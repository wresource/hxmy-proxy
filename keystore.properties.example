# 正式签名凭据模板。复制为 keystore.properties（同级目录）并填入真实值后，
# ./gradlew assembleRelease / bundleRelease 才会用正式证书签名。
#
#   cp keystore.properties.example keystore.properties
#
# keystore.properties 已在 .gitignore 中，不会入库；本模板文件不含任何真实凭据。
# 不填也能构建，只是产出未签名包（装不上、也传不了 Play）——这是有意的失败方式，
# 避免误用错误的签名。已发布版本的证书主体是 O=mzstdcom, CN=Yuhang Xie，
# SHA-1 b8:2c:e5:4e:27:8a:20:dd:2e:ad:0e:81:c8:2c:bb:8c:dd:3d:6f:44（可用来核对是否是同一把）。

storeFile=/absolute/path/to/your.keystore
storePassword=
keyAlias=
keyPassword=

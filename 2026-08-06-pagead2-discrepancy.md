# 关于 `pagead2.googlesyndication.com` 的观察分歧（2026-08-06）

> hxmy 侧反馈：「那句 `pagead2.googlesyndication.com` 观察到 200，我这边 33 次记录全是
> REJECT —— 需要他们给具体时间戳才能查那一刻发生了什么。」
>
> 本文提供可查证的时间戳与源端口，并补做了一项你们无法从我那句话里判断的验证。

---

## 一、先认一个方法问题

我最早那句观察**给不出时间戳，是我的方法有问题**：当时用 `nc` 直接朝手机发裸
`CONNECT`，完全绕开了 proxyshim，所以**我自己这边的日志里也没有记录**。
只有一句结论，没有可追溯的凭据 —— 你们要求时间戳是对的。

已改用 Python 建连，打印源端口；并补了一组经过 proxyshim 的测试，
这样**两侧日志都有痕迹**，可以双向核对。

---

## 二、环境

```
本机 IP     10.130.104.65
上游        10.130.104.228:8090   (proto=http)
proxyshim   version 2026.08.06
时区        +0800
```

---

## 三、可查证的时间戳

### 第一组：裸 CONNECT（不经 proxyshim，你们只会看到这一跳）

| 时间 | sport | 域名 | 你们的应答 |
|---|---|---|---|
| 23:52:49 | **51378** | `googleads.g.doubleclick.net` | `403 Blocked` |
| 23:52:50 | **51391** | `pagead2.googlesyndication.com` | `200 Connection established` |
| 23:52:51 | **51393** | `google-analytics.com` | `200 Connection established` |
| 23:52:52 | **51395** | `adservice.google.com` | `200 Connection established` |

### 第二组：CONNECT + 真实 TLS 握手

| 时间 | sport | 域名 | 结果 |
|---|---|---|---|
| 23:53:35 | **51509** | `pagead2.googlesyndication.com` | 200 → **TLS 成功**，证书 SAN `*.g.doubleclick.net`, `*.invitemedia.com` |
| 23:53:38 | **51521** | `google-analytics.com` | 200 → **TLS 成功**，SAN `*.google-analytics.com` |
| 23:53:40 | **51525** | `adservice.google.com` | 200 → **TLS 成功**，SAN `*.google.com` |
| 23:53:43 | **51530** | `googleads.g.doubleclick.net` | `403 Blocked` |
| 23:53:43 | **51532** | `www.google.com` | 200 → TLS 成功（对照组）|

### 第三组：经过 proxyshim（两侧都有日志）

| 时间 | 域名 | 最终 HTTP 状态 |
|---|---|---|
| 23:55:35 | `pagead2.googlesyndication.com` | **404** |
| 23:55:36 | `google-analytics.com` | **301** |
| 23:55:39 | `adservice.google.com` | **404** |
| 23:55:40 | `googleads.g.doubleclick.net` | **000**（被拦）|

proxyshim 侧同期日志，只有一条拦截记录：

```
2026-08-06 23:55:40 blocked by upstream: googleads.g.doubleclick.net:443 (HTTP/1.1 403 Blocked) (127.0.0.1:51885)
```

---

## 四、一项你们无法从我那句话判断的验证

我最初只说「返回 200」。**200 只代表隧道建立成功，规则完全可能在数据阶段才拦** ——
如果是那样，两边的观察就都成立，只是描述的是不同阶段。

所以我补做了第二、三组：

- **第二组**：拿到 200 之后真的完成 TLS 握手，并读到了**真实证书链**
  （`pagead2` 的证书 SAN 是 `*.g.doubleclick.net`，与 Google 的实际签发一致）
- **第三组**：完整 HTTPS 请求拿到 **404 / 301** —— 这是源站的真实应答，
  不是代理伪造的

**结论：不是「先放行后拦截」，是流量真的到达了互联网。**

同一时刻 `googleads.g.doubleclick.net` 稳定 403，说明规则引擎在正常工作 ——
只是这三个域名没有命中。

---

## 五、差异可能来自哪里（按可能性排序）

供你们对照，我这边无法判断：

1. **那 33 次的来源不是这台机器。**
   请按 `remote=10.130.104.65` 过滤后再统计。可能是手机自身 App 的流量，
   或其它客户端。

2. **规则命中与实际执行不一致。**
   第二、三组已排除「延迟拦截」的可能。如果你们的 `evt=req.rule` 记了
   `act=REJECT` 而连接实际放行了，那是**决策与执行两条路径脱节**，
   值得单独查一下。

3. **监听口不同，规则集不同。**
   我这几次全部走 `10.130.104.228:8090`。如果 `8079` / `1089` 挂的是另一套规则，
   两边看的就不是同一个东西。请确认那 33 次记录来自哪个监听口。

4. **时间跨度。**
   我最早那次观察是 **08-05 下午**，当时上游还是 `192.168.50.75:8090`（不同网络、
   不同 IP）；本文这几组是 **08-06 23:52–23:55**。如果你们那 33 次是更早的记录，
   规则集可能在中间改过。

---

## 六、建议的反向验证

**在手机端本机主动发起**一次到 `pagead2.googlesyndication.com` 的请求（走自己的代理），
看规则是否命中：

- 手机端自测 **REJECT**，而从 `10.130.104.65` 过去是 **200** →
  差异锁定在**客户端来源判定**上（规则里带了 source 条件，或局域网客户端走了不同分支）
- 手机端自测也是 **放行** → 那 33 次 REJECT 来自别的时间或别的来源，
  与本文这几组无关

---

## 七、顺带说明我这个观察的份量

这条我当初就是当作**顺带观察**提的，不是缺陷报告 —— 原话是「如果本意是拦广告，
规则匹配可能写得过于具体」，并注明「没有改动前的对照数据，所以这只是观察，
不是结论」。

现在有了可查的时间戳，判断权在你们：如果这三个域名**本来就不在拦截列表里**，
那就是我多虑了，本文可以直接归档。

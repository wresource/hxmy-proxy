# hxmy proxy · V1.1 UI 重构 + 规则路由/广告拦截 设计方案（2026-06-26）

> 分支 `feature/v1-implementation`。用户已批准,**Phase 1 + Phase 2 一起做完再交付**。
> 续作记忆:`~/.claude/.../memory/v1-restructure-rules-plan.md`;grounded 研究结果:workflow `w1ukb6i75`(UI 盘点 + 规则/广告源)、`wwlaxnsyv`(视频吞吐)。

## 0. 范围与目标

两件事:**(一)精简重构现有 UI**(导航 5→4 tab、主页卡片重排、诊断并入监控);**(二)新增规则路由 + 广告拦截功能**(按域名/IP/App 决定 直连 / 走代理 / 阻断)。

## 1. 已定决策（用户确认）

| # | 决策 |
|---|---|
| ① | 「规则」= **底部新 tab**;底栏 4 个:主页 / 监控 / 规则 / 设置 |
| ② | 名单语义 = **Clash 经典**:名单走**直连绕过**、其余走**代理**、广告 **REJECT** |
| ③ | 广告内置 **OISD small**、**默认关**(用户主动开;配用户白名单兜底) |
| ④ | Phase 1 + 2 **一起做完再给** |

**补充澄清:**
- **A** 可分享入口:每个接口带**开关**(可勾选),默认 2 行、超出折叠。
- **B** 目标域名:只展示 **1 行**、其余折叠(与客户端一致)。
- **C** 规则分级开关:IP/域名类 = **整组**(一堆域名/IP)一个开关;App/服务类 = **每个服务**一个开关;广告类 = **每个表**一个开关 + 用户白名单覆盖。

---

## 2. Phase 1 — UI 重构（纯 UI/状态,可单独装机验证）

### 2.1 导航 `ui/AppRoot.kt`
- `Dest` sealed class(`:43-49`):删 `Interfaces`(`:45`)、`Diagnostics`(`:46`),新增 `Rules`(`route="rules"` + `nav_rules` 文案 + `ic_nav_rules` 图标)。
- `destinations`(`:58`)→ `listOf(Dashboard, Rules, Monitor, Settings)`。
- `NavHost`(`:76-105`):删 `composable(Interfaces.route)`(`:82`)、`composable(Diagnostics.route)`(`:83`);加 `composable(Rules.route){ RulesScreen(...) }`。`startDestination`/`popUpTo` 仍指 `Dashboard`(`:78/:118`),不动。
- 删除 import:`InterfacesScreen`(`:37`)、`DiagnosticsScreen`(`:35`)。

### 2.2 主页 `ui/dashboard/DashboardScreen.kt`
卡片顺序:**① 状态(不动,`:65-102`)→ ② 可分享入口 → ③ 入口配置**。
- **③ 入口配置(原推荐入口卡 `:104-123`)**:不再硬选 SOCKS5(`:104-105`/`:112-113`)。按协议分组渲染 `recommendedEntries`,格式 **`协议  IP:端口`**(`"${entry.protocol}  ${entry.ipEndpoint}"`,`ipEndpoint` = `host:port`,`ProxyEntry.kt:23`)。**默认只展示一条 HTTP**,其余协议(SOCKS5/PAC)与多接口的多条配置折叠;一个 `TextButton` 展开/收起;默认 2 行。复制/扫码(`:114-120`/`:125-146`)随当前条目;扫码 `setupUrl`(`:126`)用 PAC 端口不变。
- **② 可分享入口(原只读块 `:148-156`)**:改**可勾选** —— 每接口一行 `Switch(checked = iface.id in selectedInterfaceIds, onCheckedChange = { viewModel.toggleInterface(iface.id, it) })`(复用 `MainViewModel.toggleInterface`,`:118-120`,与 `InterfacesScreen.kt:47-66` 的 Switch 结构一致);默认 2 行折叠。`InterfaceType.labelRes()`(`InterfacesScreen.kt:25-32`)搬到公共位置。
- 删 `InterfacesScreen.kt` 整文件。

### 2.3 监控 `ui/monitor/MonitorScreen.kt`（并入诊断）
段落顺序:**诊断 → 服务延迟 → 客户端 → 目标域名**。全部复用现成 `GridCell`(`:58-96`)/`gridSection`(`:102-129`)。
- **诊断段(新,顶部)**:数据 `ui.share.diagnostics`(`ShareState.kt:6-15/:29`),6 项(本地网络权限/VPN/HTTP·SOCKS·PAC 端口/mDNS,源 `DiagnosticsScreen.kt:26-33`)映射进 `gridSection`:圆形图标 + 一眼正常/异常(配色复用 `StatusColors.good()/bad()`,参考 `latencyColor` `:46-51`),~2 行,不堆文字。
- **服务延迟**:按延迟**升序**(小→大),刷新更新,~2 行,有图标先放、其余折叠。
- **客户端**:1 行,多余折叠。
- **目标域名**:**1 行**、多余折叠(澄清 B);按**最近转发时间**排序 —— `DomainTraffic.lastSeenAtEpochMs` 字段**已存在**(`DomainTraffic.kt:12`,热路径 `TrafficAccounting.add` `:60` 维护),UI 层一行 `ui.share.topDomains.sortedByDescending{ it.lastSeenAtEpochMs }`(`MonitorScreen.kt:234`),不动数据层。
- 删 `DiagnosticsScreen.kt` 整文件。

---

## 3. Phase 2 — 规则引擎（分流 + 广告拦截）

### 3.1 架构 · 两个落点
hxmy 是代理:客户端把请求发来后只能**转发**或**拒绝**,**无法命令客户端「直连」**。故:
- **「直连/绕过代理」只对用 PAC 的客户端有效** —— PAC 对直连名单域名返回 `DIRECT`,客户端自己直连、不经手机。手动填代理的客户端所有流量都过 hxmy(没法绕,但仍走手机出口)。
- **「阻断广告/REJECT」对所有客户端有效** —— hxmy 代理层拒绝这些域名的连接。

→ 规则引擎 = **落点① PacGenerator 注入(DIRECT/PROXY)** + **落点② 代理层 REJECT**。

### 3.2 数据模型（新 `core/rules/` 包）
- `RuleEngine`:持**后缀字典树**(域名按 `.` 切分、标签反转插入,匹配 O(标签数));`decide(host) → DIRECT / PROXY / REJECT`。
- **来源三层 + 优先级**:`用户自定义 > App 专项 > 大类(direct/proxy) > IP 兜底`;广告表并入 REJECT 层,**用户白名单覆盖优先级最高**(防误杀)。
- **PSL 边界**:合并/去重时剔除「只到公共后缀」(`co.uk`/`github.io`)的危险项,别拦整段。
- 存储:DataStore 存各组开关 / 用户规则 / 订阅 URL;内置预处理清单进 `assets`;订阅后台拉取 + 本地缓存 + 失败回退内置。

### 3.3 落点① PAC `core/proxy/PacGenerator.kt`
`generate(...)`(`:17`)加规则参数:对 host —— 直连名单→`DIRECT`、广告→黑洞/`DIRECT`(配代理层 REJECT)、其余→`PROXY <入口>`。

### 3.4 落点② 代理层 REJECT
在拿到目标 host 处调 `RuleEngine.decide(host)`,`REJECT`→拒绝(关连接 / 502):
- `HttpProxyServer.handleConnect`(CONNECT host)与 `forwardPlainHttp`(`uri.host`);
- `Socks5ProxyServer`(ATYP 解析出的 host/addr)。
- 现有 `EgressGuard.isAllowed(addr)` 是 `OutboundConnector.connectAny` 里唯一的出口过滤点,可作 IP 级 REJECT 的参考/补充挂点。

### 3.5 规则页 `ui/rules/RulesScreen.kt`（三模块,分级开关 — 澄清 C）
1. **IP / 域名名单**:整组一个开关 + 增删改(走 DIRECT)。
2. **App / 服务规则集**:每个服务一个开关(网易云/微信/Telegram/Spotify…)+ 自定义订阅。
3. **广告拦截**:每个表一个开关(默认 OISD small、**默认关**)+ 用户白名单覆盖。

---

## 4. 规则 / 广告源（已 grounded 验证;内置 GPL-3/MIT,AGPL 仅订阅）

### 4.1 分流规则
| 用途 | 源 / 格式 | raw URL | License |
|---|---|---|---|
| 国内直连 | MetaCubeX geosite `cn`(纯 `+.` 后缀,最易解析) | `…/MetaCubeX/meta-rules-dat/meta/geo/geosite/cn.list` | GPL-3 |
| 海外走代理 | geosite `geolocation-!cn` | `…/geosite/geolocation-!cn.list` | GPL-3 |
| 局域网直连 | Loyalsoldier `private` | `…/Loyalsoldier/clash-rules/release/private.txt` | GPL-3 |
| 单 App | blackmatrix7 `<App>.list`(只留 `DOMAIN-SUFFIX`/`DOMAIN`) | `…/blackmatrix7/ios_rule_script/master/rule/Clash/{NetEaseMusic,WeChat,Telegram,Spotify}/<同名>.list` | GPL-2 |

⚠️ `cn.list` 的**裸 `cn` 后缀会匹配全部 `*.cn`**(MetaCubeX issue #154),内置前需人工校验或保留代理白名单覆盖。

### 4.2 广告域名表
| 表 | 格式 | License | 用途 |
|---|---|---|---|
| **OISD small** | 纯域名 wildcard(零破坏、低误杀) | GPL-3 | **首版内置默认(默认关)** |
| HaGeZi light | `wildcard/light-onlydomains.txt`(~4.4 万域) | GPL-3 | 备选内置 |
| StevenBlack hosts | `0.0.0.0 domain` | MIT(最宽松) | 备选 |
| OISD big / HaGeZi pro / SukkaW reject | 大表/激进 | GPL-3 / **AGPL** | **仅可选订阅**(AGPL 不内置) |

⚠️ **EasyList 不是域名表**(含 CSS 隐藏规则),要用 `justdomains.github.io/blocklists` 的转换版。

### 4.3 解析归一化（统一成纯后缀清单）
- `DOMAIN-SUFFIX,x` / `DOMAIN,x` → 保留;丢弃 `IP-CIDR`/`IP-ASN`/`PROCESS-NAME`/`DOMAIN-KEYWORD`(host 匹配易误伤)与 cosmetic。
- hosts:取第 2 字段;ABP:`||x^`→后缀、`@@||x^`→白名单、含 `$`/路径/`*`/`##` 丢弃;去 `+.`/引号/`- `。
- **打包**:内置一次抓取并预处理成 `direct/proxy/reject` 纯后缀三文件进 `assets`(gzip,百 KB 级);完整 URL 作可选订阅。

---

## 5. 关键技术注意

- PAC 直连只对 **PAC 客户端**绕过;手动代理客户端仍过 hxmy(走手机出口)。广告 REJECT 对**所有**客户端有效。
- License:内置只用 **GPL-3 / MIT**;**SukkaW(AGPL)仅作订阅 URL、不打进包**;Peter Lowe 未明确授权再分发,默认不内置。App 内标注来源 + License。
- 体积:small/light 量级明文 ~1–3 MB、gzip ~200–500 KB、内存几 MB,可内置;big/pro 量级走订阅。
- **VpnDownStrategy 与规则正交**:`settings_vpn_strategy` 三态(继续走普通网络 fail-open / 阻断 fail-closed 默认 / 警告但继续)已实现,技术可行(出口跟随系统默认网,D4);它管「VPN 断了之后**整体**继续/停」,规则管「**每条**流量 direct/proxy/reject」,可共存。「继续走普通网络」是安全取舍(VPN 掉→别人流量从真实 IP 出,故默认阻断)。

---

## 6. 实施顺序

1. **Phase 1**(导航 → 主页 → 监控)—— 纯 UI/状态,改完编译 + 全单测,可单独装机验证手感。
2. **Phase 2a** —— 规则引擎后端(`core/rules/` + PSL + 白名单)+ PacGenerator 注入 + 代理层 REJECT + 内置清单预处理脚本。
3. **Phase 2b** —— `RulesScreen` 三模块 UI + 订阅更新。

## 7. 待办 / 风险

- `cn` 裸后缀过宽需校验(见 §4.1)。
- 后缀 trie + PSL 边界 + 用户白名单是防误杀三件套,务必都做。
- 订阅默认走 `cdn.jsdelivr.net`(GitHub raw 国内不稳,约 12h 延迟),允许切镜像;所有订阅带本地缓存、离线可用。
- 任务清单:TaskCreate #6–#11。

---

## 8. 实际实现与验证（2026-06-26 落地）

> 记录从设计到落地的**实际结果与对原设计的修正**。代码在 `feature/v1-implementation`,Pixel Fold（felix）真机验证。

### 8.1 关键修正:分流 = 出口分流,不是 PAC

原 §3 设想用 PAC 注入 DIRECT。落地时与用户确认后**改方案**:
- **PAC 大类分流不可行**:`cn` 清单 11.2 万条,塞 PAC 脚本会重演 iOS PAC 超时（§5 的坑），且对手动代理客户端无效。
- **真实需求是「出口分流」**:某些域名**绕过手机共享出去的那条 VPN、从手机真实网络出口**——在**代理层**做,对**所有客户端**有效,不需要 PAC。
- **实现**:`DIRECT` 域名 → `OutboundConnector` 把 upstream socket 绑定到非 VPN 底层网络（`ConnectivityManager` + `NET_CAPABILITY_NOT_VPN` + `network.socketFactory.createSocket()` + `network.getAllByName()`）;其余走默认网络。grounded 依据 workflow `wi4tuopog`。
- **三态语义最终定**:`DIRECT`=绕过 VPN 走真实网络;`PROXY`=走默认网络（含 VPN）;`REJECT`=拒绝。HTTP CONNECT / 明文 HTTP / SOCKS5 三落点统一。

### 8.2 落地功能与 commit

| 功能 | commit |
|---|---|
| 规则引擎核心（后缀树 + decide 优先级） | `fceb0bc` |
| 设置存储（enabledRuleGroups/userDirectRules/订阅URL） | `d9123c4` |
| 代理层 REJECT（server `ruleEngine?=null` 默认 null 不破坏测试 + Hilt 注入） | `81d2e10` |
| 广告拦截闭环（OISD 内置 + RuleRepository + 规则页） | `46214fe` |
| 出口分流 DIRECT（UnderlyingNetworkProvider + OutboundConnector.bypassVpn） | `b45ed51` |
| App/服务集（网易云/B站/微信，blackmatrix7） | `e0170dd` |
| 通知珊瑚色 + 线条图标 | `2a3fd94` / `3890eea` |
| Phase 1 UI 重构 | `674eef6` |

### 8.3 规则源与 License（grounded 复核 workflow `wl6ps85y6`）

- **广告 OISD small**（`https://small.oisd.nl/domainswild`,6.1 万）— **CC BY-NC-SA（非商业）**,非原以为 GPL-3。本项目开源非商业符合;商用须改 MIT（StevenBlack）。App 内注明来源 + license。
- **App blackmatrix7**（GPL-2）— 仅留 `DOMAIN`/`DOMAIN-SUFFIX`,丢 IP-CIDR/no-resolve。
- **打包坑**:AGP **自动解压 assets 里的 `.gz`**（打成无后缀同名文件），故清单**用未压缩 `.txt`**（aapt 入包 zip 压缩、运行时透明解压）。
- **防误杀三件套**:预处理剔除单段过宽项（`cn`/`+.cn` 类）+ 后缀树边界 + 用户白名单优先级最高,都做了。

### 8.4 真机验证（Pixel Fold,7 instrumented + 全单测）

- `RuleRepositoryTest`（4）:真实 assets 装载 → `googlesyndication.com`→REJECT、用户白名单覆盖→DIRECT、`music.163.com`→DIRECT、未启用→PROXY。
- `UnderlyingNetworkProviderTest`:`NOT_VPN+WIFI` callback 拿到非 VPN 网络。
- `SplitTunnelEgressTest`:**出口 IP 对比** —— 经 VPN `162.120.223.173`（海外） vs 经分流 `112.37.160.0`（手机真实移动 IP），截然不同 → **绕过 VPN 生效**。

### 8.5 已知限制

- **always-on VPN + lockdown（「阻止无 VPN 连接」）**:系统内核层丢非 tun 流量,出口分流绕不过（平台硬边界）。当前静默回退走 VPN + 记日志;**待加 UI 提示**。本次测试 VPN `bypassable=true`（未开 lockdown）故可绕。
- 大类「国内直连」（cn 全表）未内置（PAC 不可行 + 出口分流大表意义有限）;可作可选订阅。
- App 服务集仅 3 个;QQ 目录名不符未抓,可扩。

---

## 9. 规则集管理深化 + 图标 + lockdown（2026-06-26 续）

> §8 之后用户继续提的需求,均已实现 + 真机/模拟器验证。

### 9.1 规则集 = 一等公民（查看 / 编辑 / 自建）
- **管理界面** `ui/rules/RuleSetManagerScreen.kt`:我的规则集（自建命名集——增删集 + 改名 + 开关 + 删除）+ 内置规则集（只读,「查看域名」展开 preview）。规则页保持简单（开关 + 「管理规则集」入口）。
- **自建命名集** `core/rules/UserRuleSet.kt`(id/name/action/domains/enabled):动作 DIRECT(出口分流)或 REJECT;**优先级高于内置**(防误杀)。存 `ProxySettings.userRuleSets`,DataStore 用 **org.json** 编解码(项目无 kotlinx.serialization 依赖)。
- decide 优先级升级:`RuleEngine.Snapshot` 加 `userReject`;顺序 = userDirect > userReject > 内置reject > 内置direct > 内置proxy > 兜底PROXY。
- commit `d1625f3`。真机 `RuleRepositoryTest` + `SettingsRepositoryInstrumentedTest`(org.json round-trip)。

### 9.2 批量文本编辑 + 内置覆盖 + 导入导出
- **编辑界面** `RuleSetEditScreen.kt`:多行文本框(一行一域名)整体编辑,替代逐条增删。
- **内置集可编辑**(≤100行,网易云/微信;`RuleGroup.editable`):改动存 `ProxySettings.ruleSetOverrides`(groupId→域名,org.json);`RuleRepository.rebuild` 有覆盖用覆盖、否则读 assets;给「恢复默认」删覆盖。OISD/B站(>100行)只读 + 导出。
- **导入/导出 txt**(SAF CreateDocument/OpenDocument):导出一行一域名;导入弹「追加去重 / 整个替换」。
- commit `6cbcb12`。真机 `builtinOverrideReplacesAsset`(覆盖替换 assets)。

### 9.3 图标（避免商标:用通用语义图标 / 品牌色,不复现 logo 图形）
- **规则集**:圆形网格,内置 App 集用 **Material 开源语义图标**(Apache-2.0)——网易云→音乐符(红)、B站→视频(粉)、微信→聊天(绿)、自建集→标签;开=品牌色圆+白图标、关=灰。commit `bd95a35`。
- **延迟测试**:每服务 **品牌识别色圆 + 白首字母**(GitHub/Google/OpenAI/Claude/Cloudflare/YouTube/Bing/Wikipedia/X/npm/HuggingFace/Docker 的公开品牌主色);延迟快慢改由数值颜色(绿/黄/红)体现。commit `45b49bd`。
- **决策**:真品牌 logo 是各公司注册商标,不直接打包(同 OISD license 的谨慎);语义图标/品牌色既好看又无版权风险(用户确认)。

### 9.4 lockdown 检测 + 提示
- 系统「阻止无 VPN 连接」(always-on VPN lockdown)下出口分流绕不过(§8.5 硬边界)。
- **检测**:`ProxyServerRepository` 代理起动后探活——经底层网络(绕VPN)与默认网络分别连国内可达 `223.5.5.5:53`(用国内 IP 避免被墙误报);底层连不通但 VPN 通 → `ShareState.lockdownSuspected=true`。
- **提示**:规则页顶部 errorContainer 警告卡,引导去系统 VPN 设置关闭。commit `2df7f9c`。编译+单测过;触发需真机开 lockdown 验证。

### 9.5 修复:instrumented 测试污染用户真实设置
- `SettingsRepositoryInstrumentedTest` 用真实 DataStore,原 `finally` 重置成默认 → **清掉用户建的规则集/选的接口**(用户实际遇到、误以为「app 没装/状态不对」)。改为测前存 `original` 快照、测后恢复。commit `e0401aa`。

### 9.6 验证环境与坑
- 真机 Pixel Fold(felix):规则引擎/装载/出口分流 instrumented + UI 截图。
- 模拟器 Pixel 10 Pro:图标(规则集 Material 图标开关变品牌色、延迟品牌色圆)UI 验证——真机 adb 临时断开时改用。
- ⚠️ **坑**:adb install / connectedAndroidTest 装的 app 图标默认进**应用抽屉**、不一定在主屏 → 曾误判「没装」(实际 `flags=0x0` 非 testOnly、launcher 入口正常)。

# 关于 503 + Retry-After：shim 侧的答复与改动

**给 hxmy。** 你们问的是：

> shim 若把 503 排除在 `upstream_5xx_threshold` 之外（或按 Retry-After 等待）→ 不判定上游死亡 → 不进 none-found → 不退避，问题不再发生
> shim 若仍按「所有 5xx」统计 → 改动无效，只是语义变正确了
>
> 你说 shim 侧已经改完了，但我不知道它现在怎么处理 503。这一点需要你确认——否则手机侧改了也白改。

问得对，**之前那句「shim 侧已经改完了」指的是别的事**（IPv6 authority 解析、按需重新发现），
不包括 503。下面是实测出来的原状况、以及为此做的改动。

---

## 一、改之前的真实行为：你们的改动会白改，而且比你们设想的更糟

### 1. 所有 5xx 一视同仁，503 没有任何特殊处理

实测（每个码单独测，冷却计数也清零）：

```
连续 3 次 200 -> 计数器清零，不触发
连续 3 次 302 -> 计数器清零，不触发
连续 3 次 403 -> 计数器清零，不触发
连续 3 次 429 -> 计数器清零，不触发
连续 3 次 500 -> 计数器累加，触发重新发现
连续 3 次 502 -> 计数器累加，触发重新发现
连续 3 次 503 -> 计数器累加，触发重新发现      ← 和 502/504 完全一样
连续 3 次 504 -> 计数器累加，触发重新发现
```

`Retry-After` 在整个代码库里**一次都没出现过**，从不读取。

所以按你们自己的判据：**改动无效。**

### 2. 更严重的一条：`validate()` 只认字面 200

shim 判断「这个端点是不是代理」的函数，对任何非 200 都返回 False：

```
200 Connection established   -> True
403 Blocked（规则拒绝）       -> False   ← 判定「这不是代理」
503 + Retry-After（摘网）     -> False   ← 判定「这不是代理」
502 Bad Gateway              -> False
504 Gateway Timeout          -> False
405 Method Not Allowed       -> False
```

它每 60 秒被 `periodic_recheck` 调用一次来复检当前上游。所以摘网期间你们回 503，
shim 走的**不是**「累计 5xx 到阈值」那条路径，而是更早、更彻底的一条：

```
periodic_recheck -> validate(手机) -> 503 -> False
                 -> invalidate("periodic-dead")   ← 直接丢弃上游
                 -> discover() 找不到别的         ← none-found
                 -> misses++                     ← 进入退避
```

退避是 `min(recheck_interval × 2^misses, recheck_max)`，默认
`60 → 120 → 240 → 480 → 600` 封顶，**15 分钟就退到 10 分钟一次**。
你们摘网恢复之后，最坏要等 10 分钟 shim 才会重新发现手机。

（顺带印证：本机 `periodic-dead` 触发过 15 次。8-17 13:51 有一次是同一台设备上
`8090/http` 被判死、`1089/socks5` 还活着 —— 但日志分不清是「没接受连接」还是「回了非 200」。）

---

## 二、shim 侧已做的改动（版本 `2026.08.18.1`）

### 1. `validate()` 分严格 / 宽松两档

它有两个调用点，问的其实是两个不同的问题：

| 调用点 | 真正的问题 | 需要什么证据 |
|---|---|---|
| 发现**新候选**（`scan`） | 这个陌生端点是代理吗 | **必须 200**。普通 web 服务器对 CONNECT 回 400/405，认错就等于把全部流量导进黑洞 |
| 复检**当前上游**（`periodic_recheck`） | 我已经找到的那个代理还在吗 | **任何 HTTP 应答**都算。它用代理协议回话了，这个问题就已经回答了 |

**这一条是你们那版能生效的前提。** 没有它，只改 `note_upstream_status` 也没用 ——
`validate` 会先一步把手机踢掉。

### 2. 503 不再计入「上游不健康」

判据现在是：

- `4xx` → 规则决定，代理是健康的 → 清零
- **`503` → 代理在，此刻主动拒绝 → 清零**
- 其它 `5xx`（500 / 502 / 504）→ 代理够不到目标，这是对它自身连通性的陈述 → 累加，
  连续 3 次触发一次重新发现

### 3. 读取并遵守 `Retry-After`

从上游响应里取 `Retry-After`（**只认 delta-seconds 形式**；HTTP-date 形式合法但没人在这里用，
而且要猜时钟偏差，宁可忽略）。取到之后：

- 窗口内不触发 5xx 重新发现
- 窗口内 `periodic_recheck` **不复检也不拆上游** —— 你们已经说了什么时候回来，扫局域网给不出更好的答案
- **原样转发给客户端**。以前 `refusal_response()` 重建响应时只带 `content-length`，会把它丢掉
- 上限 120 秒（`retry_after_max`），免得一个坏值把发现关掉一小时

`proxy_status` 新增 `upstream_retry_after_seconds`，为 0 表示不在窗口内。

### 4. 客户端看到的状态码就是你们发的

这条其实是 `2026.08.17` 做的，一并说明：以前**所有非 200 都被压成 `403 Forbidden`** 再给客户端。
现在 4xx/5xx 原样转发。所以你们回 503，客户端看到的就是 503；回 502，看到的就是 502。

> 这也意味着：任何「从客户端看到 403」出发的排查，在 `2026.08.17` 之前都是从错误前提开始的。

---

## 三、这个约定的确切语义

**503 必须表示暂时。** shim 收到 503 会认为「手机在，等一下就好」，不重新发现、不退避、
继续守着它。如果你们拿 503 表达一个永久性故障，shim 会一直守着一个不可用的上游不动。

**永久性 / 目标侧的失败仍然用 502。** shim 对 502 的处理不变：连续 3 次触发一次重新发现。

你们提到要加测试锁住「摘网返回 503 且带 Retry-After」和「目标不可达仍返回 502」必须能区分开 ——
**这正是 shim 这侧依赖的区分**，我方也加了对应测试（`tests/test_503.py`）：

```
=== validate()：复检当前上游 vs 发现新候选 ===
  复检当前上游  200 / 403 / 503 / 502 / 405   -> 全部 True（都是「它还在」）
  发现新候选    200 -> True；403 / 503 / 502 / 405 -> False

=== note_upstream_status() ===
  连续 3 次 200 / 403 / 429 / 503 -> 不触发（上游是健康的）
  连续 3 次 500 / 502 / 504       -> 触发重新发现

=== Retry-After ===
  504 带 Retry-After: 30 -> 窗口内不触发重新发现
  转发给客户端时保留 Retry-After，没有时不凭空造

=== 端到端 ===
  http_connect 从 503 响应里取出 status=503、retry_after=12
```

---

## 四、你们说「不建议一起改」的两处，我方同意，且不需要 shim 配合

### no-physical-egress 的 403 保持不变

同意，理由和你们一致，而且 shim 侧本来就配合得上：**403 一直是清零健康计数的**
（视为「代理正常工作」的证据），并且自 `2026.08.17` 起原样转发给客户端。
策略拒绝的语义强度不会被削弱。

### 摘网期间快速失败，不做试探性放行

同意。补充一条你们可能不知道的：自 `2026.08.17.1` 起，shim 在**没有上游**时，
请求本身会驱动重新发现（下限默认 15 秒），不再只靠退避定时器。

之前的行为是：一轮空发现之后，`get_upstream()` 永远直接返回「没有」，
**任何数量的请求都不会让它去看一眼**。实测 41 个请求 / 40 秒全部 502，
那 40 秒里 `discover()` 只被调用 1 次，而且是定时器调的。

所以你们那边**不需要再加补偿层**，重试的驱动力已经在 shim 这边了。

---

## 五、还想请你们确认的一件事

**手机侧现在对 IPv6 目标有出口吗？**

之前我方发出去的 CONNECT 行对 IPv6 是畸形的（没有方括号，
`CONNECT 2409:8c6a:b021:1400:0:4:0:3:443`，你们无法分辨地址在哪结束、端口从哪开始），
32 次 IPv6 请求全部 504 —— 那大概率是我方的错。`2026.08.17` 已修正为
`CONNECT [2409:8c6a:b021:1400:0:4:0:3]:443`。

**请重新试一次。** 如果当前网络确实没有 IPv6 出口，回 504 是对的；
将来有了会自动生效，shim 这边不需要再改（我们刻意没有把 IPv6 写死成直连）。

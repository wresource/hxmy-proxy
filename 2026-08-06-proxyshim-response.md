# proxyshim 侧的回应与改动（2026-08-06）

> 针对 hxmy 侧两份文档（《给 proxyshim 的问题反馈》《端到端链路分析》）。
> 每条都对着 `~/Library/Logs/proxyshim.log`（5460 行，07-07 → 08-06）核实过，
> 结论与你们不完全一致的地方会写明依据。
>
> **版本：`2026.08.05` → `2026.08.06`**。`proxy_status` 和启动日志都会打印版本号，
> 可据此确认对方跑的是哪一版。

---

## 一、结论修正：主因还要往前推一环

你们的结论「主因在 proxyshim 侧」**成立**，但根因不是 `on_no_proxy: direct` 本身。

真实链条是**两个缺陷叠加**：

```
切换网络服务（USB / 热点 / 网线 / VPN 接口）
   └─ 那个服务的代理面板是空的（各服务的代理配置本来就是隔离的）
      └─ ① proxyshim 把「面板全空」误判成「禁止使用任何传输」
         └─ 候选表被过滤成空 → 发现链无从下手 → 无上游
            └─ ② on_no_proxy=direct → 自己直连 → 自己解析域名 → gaierror
```

**`direct` 只是把故障伪装了，制造「无上游」状态的是 ①。** ① 是我们这边 08-04 引入的，
你们那份文档没看到这一层，因为它只在日志里留下一行不起眼的
`protocol gate: skipping [...]`。

### 证据

四次「三个候选被全部拦掉」的时刻，**无一例外落在断网期内**：

| 时刻 | 无代理持续 |
|---|---|
| 08-04 18:24:45 | 1767 秒（29 分钟）|
| 08-05 13:44:39 | 624 秒 |
| 08-05 15:38:40 | 12 秒 |
| 08-06 10:08:58 | 841 秒（14 分钟）|

累计约 **54 分钟无代理**。这期间手机端完全正常。

本机 8 个网络服务里**只有 Wi-Fi 配了代理**，其余 7 个（Thunderbolt Bridge、iPad USB、
iPhone USB、Tailscale…）面板全空 —— 切到任何一个都会触发。

---

## 二、逐条回应

### ① `on_no_proxy: "direct"` — 已改为 `"fail"`

**你们说得对。** 实测复核：

- `gaierror` 共 **130** 次（不是 120），`api.anthropic.com` 占 **55** 次
- **我方 08-05 修复后仍在发生**（08-05 有 28 次，08-06 有 13 次），确认不是历史遗留

补充一个你们没提到的细节：`10:09:18` 一秒内 7 个域名同时 gaierror，
**其中包含国内域名 `rmonitor.qq.com`** —— 说明不在绕行列表里的国内站点同样中招，
不只是境外域名。

已改为 `"fail"`。实测行为：

| 目标 | 无上游时 |
|---|---|
| 绕行列表内（知网、163…）| 照常直连 ✅ |
| 非绕行 · 境外 | 明确 `502 proxyshim: no upstream proxy found` |
| 非绕行 · 国内 | 同上 |

**对你们的影响**：以后 proxyshim 找不到上游时，客户端拿到的是明确的 502，
不会再表现为「域名不存在」。如果你们那边看到流量突然归零而不是报错，
那才是真的没到达。

### ② 5xx 不计入上游失败 — 已修

**你们说得对，而且实际比重更清楚了。** `blocked by upstream` 共 3457 条：

| 状态码 | 次数 | 原判定 | 新判定 |
|---|---|---|---|
| 403 | 3160 | 上游正常 ✅ | 上游正常（不变）|
| **504** | **265** | 上游正常 ❌ | **计入上游失败** |
| **502** | **7** | 上游正常 ❌ | **计入上游失败** |

现在的判据：

- **4xx** = 规则决策，是上游健康的证据 → 计数清零
- **5xx** = 上游够不着目标 → 累计，连续 **3 次**触发一次重新发现，**60 秒冷却**

实测验证：403 连来 10 次不触发；504 连来 3 次触发一次；冷却期内再来 10 次不重复触发；
中间夹一次 403 会清零连击。

**刻意没有做的事**：没有对同一目标做客户端侧退避/限流。proxyshim 的设计边界是
**1:1 直通** —— 不做连接池、不复用、不限流，手机看到的连接数与没有 shim 时完全一致。
客户端的重试是客户端的事，我们只把「上游健康」这个信号用起来。
如果你们希望 shim 替客户端做退避，那是另一个设计决定，需要单独讨论。

### ③ `Too many open files` — 已修，且是历史记录

这条**不是现存问题**：

- 全部 8 条集中在 `2026-08-04 11:44–11:48`
- 那是我们做并发压测的窗口（自己压出来的）
- **当天下午就修了**：plist `SoftResourceLimits/NumberOfFiles=8192` + 代码里
  `raise_fd_limit()` 双保险
- 08-05、08-06 各 **0 次**

根因是 launchd 给 agent 的默认软限制 256，而每条隧道占 2 个 fd —— 约 100 并发就撞墙，
**症状只表现为「连接被重置」**，没有任何提示说撞了限制。修复后实测 400 并发 400/400。

### ④ 发现流程的成本 — 已重构，结论反转

你们引用了我们文档里「每多一个端口最坏多约 5 秒」——**那句话现在作废了**。

那是实现缺陷（`scan()` 端口在外层循环，先用端口 1 扫遍全网段，不中再用端口 2 扫一遍），
不是固有代价。已改成**一次扁平并发**扫完所有 (主机, 端口) 组合；全网段那轮分三段：

| 阶段 | 扫什么 | 为什么 |
|---|---|---|
| pass 1 | 253 主机 × **只有首选端口** | 主机数是贵的那半，只为一个端口买单；顺带热 ARP |
| pass 2 | **ARP 存活的少数主机** × 全部候选 | 主机少了，多扫端口几乎免费 |
| pass 3 | 253 × 全部候选 | 兜底 |

实测：

| 候选数量 | 命中邻居轮 | 最坏（全网段）|
|---|---|---|
| 1 个 | 1.13s | 3.58s |
| 8 个 | 1.13s | 3.30s |
| 16 个 | 1.44s | — |

**1→16 个候选只多 27%**（旧结构约需 18 秒）。

关于「不开手机 app 时电脑变慢」：修复后「完全找不到代理」的整条链耗时
**2.56 秒**，且连续找不到时按 60→120→…→600s 退避。这个量级不足以解释可感知的卡顿，
建议换个方向排查。

### ⑤ 日志对齐 — 已补源端口

`blocked by upstream` 和 `upstream ... failed` 两类日志现在都带上了源端口，
可以直接和你们的 `sport=` 对齐：

```
2026-08-06 13:20:43 blocked by upstream: googleads.g.doubleclick.net:443 (HTTP/1.1 403 Blocked) (127.0.0.1:62336)
2026-08-06 ..:..:.. upstream 192.168.1.104:8090/http failed for CONNECT xxx (timeout) -- rediscovering (127.0.0.1:51207)
```

**用「时间 + 源端口」即可两侧拼接。**

---

## 三、我们这边另外修的（对你们可见的部分）

这些不在你们的文档里，但会改变你们观察到的现象：

### 发现风暴（08-05 修）

`invalidate()` 原本恒用 `force=True`，**绕过了单飞保护**。手机代理挂掉时，
每一条在途连接各自发起一轮完整发现：

```
20 条连接同时失败 → 并发 discover 峰值 20 → 峰值 20 × 64 = 1280 个 TCP 探测同时打向局域网
```

改成带 generation 计数的真单飞后：**峰值 1**，探测降到 64。

**这大概率就是你们观察到的「重试风暴」的一部分** —— 但它打的是局域网扫描，
不是打你们的代理端口。你们那边如果在故障期间看到密集的连接尝试，
现在应该会明显减少。

### 转发收尾导致响应截断（08-05 修）

`pump()` 原本在任一方向结束时对两个 socket 都做 `shutdown(SHUT_RDWR)`。
关闭读方向时若还有未读数据，内核发 RST，**而 RST 会丢弃发送缓冲区里的内容** ——
正在传的响应被截断。低负载不显形，200 并发时 **27/200 响应不完整**。

**如果你们那边统计过「上游主动断开」或「响应长度异常」，之前可能有一部分是这个造成的。**

### SNI 截断（08-05 修）

SOCKS 客户端自己解析 DNS 时，我们从 ClientHello 里把域名找回来再转给你们。
原实现的切片没做长度校验，Python 切片越界不抛异常而是静默截断 ——
`www.example.com` 可能被截成 `www`，**无点主机名会被判为内网直连，完全绕过你们的代理**。

已加长度校验，实测任意长度截断均返回 `None`（宁可不恢复也不猜）。

---

## 四、拓扑与排障方法（回应《端到端链路分析》）

你们 §6 那套「先确认拓扑、有中间层必须两侧同时抓」的方法论我们完全同意，
补充三点从我们这侧看到的：

1. **失败次数与用户感知不是一一对应** —— 你们已经指出。补一个量化：
   我们侧「上游失败 → 重发现 → 重试」的端到端耗时实测 **1.57 秒**，
   这期间客户端通常无感。所以你们日志里的一次失败，用户可能完全不知道。

2. **`blocked by upstream` 不是错误** —— 3457 条里 3160 条是 403 规则拦截，
   属于你们正常工作的证据。排障时应该按状态码分开统计，别把它当异常量。

3. **我们的日志现在带版本号**，出问题时先确认版本：

   ```
   === proxyshim 2026.08.06 up: http 127.0.0.1:18790, socks5 127.0.0.1:18791, ... ===
   ```

---

## 五、还请你们这边配合的

1. **`proxy closed during CONNECT`** —— 你们已确认是外层超时取消协程直接关连接导致，
   客户端拿到半截握手。你们文档里列为待改，我们这边确认这个现象仍在（日志可查）。
   **建议超时时回一个干净的 504 再关，而不是直接断。** 我们已经能区分 5xx 和 4xx 了，
   干净的 504 会被正确计入上游健康判定；半截握手只能归为传输层错误，信息量更少。

2. **规则命中信息** —— 如果响应里能带上命中了哪条规则（自定义头，如
   `X-Proxy-Rule: adblock/easylist`），排障成本会从「反复二分测试」降到「看一眼」。

3. **广告规则覆盖面**（顺带观察，不确定是否有意为之）：
   `googleads.g.doubleclick.net` 会被拦（403 Blocked），但
   `pagead2.googlesyndication.com`、`google-analytics.com`、`adservice.google.com`
   都是 200 放行。如果本意是拦广告，规则匹配可能写得过于具体。

---

## 附：改动清单（2026-08-06）

| 改动 | 对你们的影响 |
|---|---|
| 协议门：面板空白不再视为禁止 | 切网时不再出现「无上游」黑洞 |
| `on_no_proxy` → `fail` | 找不到上游时明确 502，不再伪装成 DNS 失败 |
| 5xx 计入上游失败（阈值 3 / 冷却 60s）| 你们回 504 时我们会重新发现，而不是当作正常 |
| 日志补源端口 | 可与 `sport=` 对齐 |
| `scan()` 扁平化 + 三段式全网扫 | 候选端口数量不再影响发现速度 |
| 版本号 `2026.08.06` | `proxy_status` / 启动日志可查 |

08-05 已改、同样影响你们观察的：单飞发现（风暴 20 轮 → 1 轮）、转发半关闭
（消除响应截断）、SNI 长度校验（消除静默直连）、控制接口鉴权。
